Quickstart has been installed.  To complete the image:

1) Configure Netbeans and Eclipse:

    NETBEANS: 
      Install updates (popup)
      Tools->Options->General, web browser->Firefox
                    ->Editor->Formatting.  Spaces per indent: 2, Tab size 2
      Close
      Open again and Install updates (popup)
                    
    ECLIPSE:
      Set workspace to /home/quickstart/websites - use as default
      Help->Check for updates
      Window->Preferences->General->Web Browser, use external browser.  Add firefox (Firefox, /usr/bin/firefox, %URL%)
                         ->PHP->Debug, set the default to Xdebug.
                         ->General->Editors->Text Editors.  Displayed tab width: 2, Check insert spaces for tabs

2) Update firefox plugins 

3) Prepare for export:
   bash -x ~/quickstart/export-prep.sh

4) Shutdown

5) Export virtual machine

6) Create torrent

