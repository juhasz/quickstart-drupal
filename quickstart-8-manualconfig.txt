Quickstart has been installed.  To complete the image:

1) Configure Netbeans and Eclipse xdebug:

    NETBEANS: 
      Install updates (popup)
      Seems to work ootb

    ECLIPSE:
      Help->Check for updates
      Window->Preferences->General->Web Browser, use external browser (default system web browser)
      Window->Preferences->PHP->Debug, set the default to Xdebug.
 
2) INSTALL EXTENSIONS AGAIN  (updating to virtual kernel blew away old copy)

3) Shutdown

4) Export virtual machine

5) Create torrent

